# Build output and captured pcap corpora -- large, regenerable, exceed GitHub's 100MB file limit.
# Regenerate via the deploy scripts under features/p1-ran-baseline/helpers/ rather than committing.
.build/
artifacts/
__pycache__/
*.pyc
