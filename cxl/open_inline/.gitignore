build/
.build/
__pycache__/
*.pyc
*.o
*.a
*.so

# Generated live-capture artifacts (pcaps, ledgers) — large, binary, reproducible via the
# documented runbooks (DEFERRED_LIVE_GATES.md) rather than carried in git history.
artifacts/
